# 使用说明（给下载者 / AI 工作区用法）

本工具**不内置 AI**。它只负责把加密的 QQ 聊天记录变成普通 Markdown，
“分析”这一步由**你的 AI 工作区**完成。以下是最小可用的工作流。

---

## 一、机器准备（一次性）

1. Python 3.10+，装依赖：

   ```bash
   pip install sqlcipher3
   pip install -e .          # 在本仓库目录内执行
   ```

2. 拿到 QQ 数据库密钥：
   - 方法 A：QQ 登录状态下运行
     [QQBackup/qq-win-db-key](https://github.com/QQBackup/qq-win-db-key)（或其 fork）得到 key；
   - 方法 B：如果你之前已经用本工具导过，直接复用 `NTQQ_DB_KEY`。

## 二、导出（每次想更新时做一遍）

```bash
# 1) 退出 QQ（重要：保证文件一致）
# 2) 复制数据目录
xcopy "C:\Users\<你>\Documents\Tencent Files\<QQ号>\nt_qq\nt_db" .\ntdb /E /I
# 3) 导出
set NTQQ_DB_KEY=<key>
qq-history-export --ntdb-dir .\ntdb --uin <你的QQ号> --out .
```

成功后在 `chats/` 下出现 `_index.md` 和按人/群命名的 `.md`。

## 三、让 AI 分析（关键步骤）

把 **整个项目目录**（重点是 `chats/`）放到你的 AI 工作区，
例如 DeepSeek Harness 的工作目录，然后**直接向 AI 提问**，提问时**务必带上**：

- **网名/群名**（告诉它看哪份文件）
- **时间范围**（它检索时缩小范围，避免读错段落）

推荐提问模板：

> 请读取工作区 `chats/` 里与「六谷」的聊天记录（`_index.md` 里有对应文件名），
> 时间范围 2026-09-01 之后，分析：他说的观点有没有道理、理由是什么、与我相关的部分是什么。

对多个人/群：

> 分别读取与「A」和「B」的聊天记录，时间范围 X ~ Y，找出他们提到「C」的段落并对比。

---

## 四、如何拿到“最新消息”（重要细节）

导出结果 = **最后一次“复制数据库快照”时** 本地库里已有的内容，不是实时的。想更新到最新：

1. **退出电脑 QQ**（托盘退出，约 10 秒）——让 QQ 把内存/缓存写回本地库；
2. 重新复制 `nt_db` 目录（覆盖旧的 `.\ntdb`）；
3. 重新运行导出命令（同上第二节）。

三条边界，请注意：

- **手机 QQ 独有、未同步到电脑的消息，电脑本地库没有**：请先在手机 QQ
  「设置 → 通用 → 聊天记录备份与迁移 → 备份聊天记录到电脑」导出 `.bak`，
  或在电脑 QQ 里“恢复/导入”，同步后再按上面流程导出；
- 本工具是**读本地库**，不是实时监听——QQ 在线期间新到的消息，等它落库（下次退出/同步）后再导；
- 已撤回、阅后即焚、以及只存在于云端的老历史，本地库也没有。


## 五、隐私与红线

- `chats/` 是**明文**：不要把它上传到公共仓库、云盘或聊天工具；
- 本工具与导出的文件**只处理你自己的账号数据**；
- 分析结果若涉及他人，请注意边界，勿用于骚扰或公开。

## 六、常见问题

- **为什么导不出最新的消息？** 见第四节：先退出 QQ → 重新复制 nt_db → 重新导出；
  手机独有消息请先用手机 QQ 备份到电脑/漫游同步。
- **某个对话没导出**：v0.2 覆盖私聊（C2C）全量、群聊尚未支持（v0.3 计划）；
  请先确认 QQ 退出后再复制目录重导；若目标对话在手机端独有，先用手机 QQ 备份/漫游同步。
- **文件名乱码/太长**：已自动做安全化处理，可用 `_index.md` 对照原名。
- **密钥哪里来**：见 README，密钥与聊天数据一样敏感，请勿提交到 git。
